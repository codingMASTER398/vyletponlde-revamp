<div id="loaderWrap">
  <div id="loader">
    <div class="blocks">
      <div style="background: red"></div>
      <div style="background: green"></div>
      <div style="background: blue"></div>
    </div>
    <p class="loadingUA">.</p>
    <p class="loadingText">loading dom content...</p>
  </div>
</div>

<style>
  #loaderWrap {
    width: 100vw;
    height: 100vh;
    position: fixed;
    top: 0;
    left: 0;
    background-color: white;
    z-index: 999;

    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;

    font-size: small;
    font-family:
      monospace,
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
  }
  #loaderWrap.dark {
    background: #00000b;
    color: white;
  }

  #loaderWrap.out {
    animation: loaderOut 1s linear forwards;
  }
  @keyframes loaderOut {
    100% {
      opacity: 0;
      appearance: none;
      display: none;
      pointer-events: none;
    }
  }

  #loader {
    max-width: 70%;
    width: 600px;

    animation: loaderIn 0.1s;
  }

  @keyframes loaderIn {
    0% {
      opacity: 0;
      transform: scale(0.9);
    }
  }

  #loader .blocks {
    aspect-ratio: 16/1;
    display: flex;
    justify-content: space-evenly;
  }

  #loader .blocks div {
    height: 100%;
    width: 100%;
  }

  .vexiScare {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 1000px;
    animation: vexi 1s forwards cubic-bezier(0.16, 1, 0.3, 1);
  }

  @keyframes vexi {
    0% {
      transform: translate(-50%, -50%) scale(0);
    }
  }
</style>
